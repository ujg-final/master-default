Readme file is in text now. Ideally, we should render this as plain text 

```matlab
function ave = average(x)
    ave = sum(x(:))/numel(x); 
end
```

https://www.youtube.com/watch?v=j6ow-UemzBc&ab_channel=InfoQ
